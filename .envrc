# Automatically activate Python virtual environment
# Works with all shells: bash, zsh, fish, etc.

# Activate the virtual environment
source venv/bin/activate

# Load .env file if it exists
dotenv_if_exists

# Optional: Set any additional environment variables
# export FLASK_ENV=development
# export FLASK_DEBUG=1

# Print confirmation
echo "🔧 refitd-etl environment activated (Python venv + .env loaded)"
